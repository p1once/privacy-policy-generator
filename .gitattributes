# Normalize all text files to LF in repo, convert to CRLF on Windows checkout
* text=auto

# Explicitly mark binary files to skip any EOL conversion
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.zip   binary
*.gz    binary
*.pdf   binary
